services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - APP_ENV=docker-compose
      - MONGO_URI=mongodb://mongo:27017/tasksdb
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db